package com.aibrain.app.view

/** Tipos de memória usados pelo workspace e pela tela dedicada de memória. */
enum class MemoryType {
    NOTE,
    DECISION,
    ARCHITECTURE,
    PROBLEM,
    SOLUTION
}
